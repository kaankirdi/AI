# Core
numpy>=1.24

# Deep learning (Day 2+). CPU wheels work for reading the code;
# 4-bit / GPTQ / AWQ examples need a CUDA GPU.
torch>=2.1
transformers>=4.40
accelerate>=0.30

# Quantization backends (GPU examples)
bitsandbytes>=0.43     # Day 2 — NF4 4-bit
# auto-gptq>=0.7       # Day 3 — GPTQ (uncomment on a CUDA machine)
# autoawq>=0.2         # Day 3 — AWQ  (uncomment on a CUDA machine)

# Retrieval (Day 6). CPU-friendly, ~90 MB model on first run.
# sentence-transformers>=2.7   # rag_semantic.py (rag_from_scratch.py needs only numpy)

# RAG agent (Day 7). transformers+torch already above; the from-scratch
# agent needs only numpy. rag_agent_llm.py downloads a ~0.5B instruct model.
